{% import '@SyliusUi/Macro/buttons.html.twig' as buttons %}
{% set path = options.link.url|default(path(options.link.route|default(grid.requestConfiguration.getRouteName('notify')), options.link.parameters|default({'subject_id': data.id}))) %}

{{ buttons.create(path, action.label, true, data.id) }}
